/* Javascript for FileStorageXBlock. */
function FileStorageXBlock(runtime, element) {


    $(function ($) {
        /* Here's where you'd do things on page load. */
        require.config({
            paths: {
                "onedrive": "https://js.live.net/v5.0/OneDrive"
            }
        });
    });
}
